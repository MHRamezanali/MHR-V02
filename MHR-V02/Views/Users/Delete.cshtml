@model MHR_V02.ViewModels.Base.UserViewModel

@{
    ViewData["Title"] = @ViewData["Delete"];
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="glass-container  col-md-4">
    <h1 class="glass-heading">@ViewData["Delete"]</h1>
    <h3 class="glass-description">@ViewData["Areyousureyouwanttodeletethis"]</h3>
    <div class="glass-content mt-4">
        <h1 class="glass-title">@ViewData["Users"]</h1>
        <hr />
        <dl class="row">
            <dt class="col-sm-10 glass-label">@ViewData["FirstName"]</dt>
            <dd class="col-sm-10 glass-content-text">@Html.DisplayFor(model => model.FirstName)</dd>
            <dt class="col-sm-10 glass-label">@ViewData["LastName"]</dt>
            <dd class="col-sm-10 glass-content-text">@Html.DisplayFor(model => model.LastName)</dd>
            <dt class="col-sm-10 glass-label">@ViewData["Email"]</dt>
            <dd class="col-sm-10 glass-content-text">@Html.DisplayFor(model => model.Email)</dd>
            <dt class="col-sm-10 glass-label">@ViewData["ActiveStatus"]</dt>
            <span class="glass-badge @(Model.IsActive ? "glass-badge-success" : "glass-badge-danger") glass-content-text col-sm-3">
                @(Model.IsActive ? @ViewData["Active"] : @ViewData["Inactive"])
            </span>
        </dl>
    </div>

    <form asp-action="DeleteConfirmed" class="mt-4">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="@ViewData["DeleteConfirmed"]" class="btn btn-danger glass-btn-danger" />
        <div class="text-center">
            <a asp-action="Index" class="glass-link">@ViewData["BacktoList"]</a>
        </div>
    </form>
</div>
